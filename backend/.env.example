# Database (PostgreSQL pour production)
DATABASE_URL="postgresql://user:password@host:5432/transport_token?schema=public"

# JWT
JWT_SECRET="votre-secret-jwt-min-32-caracteres"

# Server
PORT=4000
NODE_ENV=production

# Frontend URL (pour CORS)
FRONTEND_URL="https://transport-token.vercel.app"
